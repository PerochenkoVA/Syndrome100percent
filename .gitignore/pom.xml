<? xml version = " 1.0 " encoding = " UTF-8 " ?>
< проект  xmlns = " http://maven.apache.org/POM/4.0.0 "
         xmlns : xsi = " http://www.w3.org/2001/XMLSchema-instance "
         xsi : schemaLocation = " http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd " >
    < версия модели > 4.0.0 </ версия модели >

    < groupId > ru.netology </ groupId >
    < artifactId > статистика </ artifactId >
    < версия > 1.0-SNAPSHOT </ версия >

    < свойства >
        < maven .compiler.source> 11 </ maven .compiler.source>
        < maven .compiler.target> 11 </ maven .compiler.target>
        < проект .build.sourceEncoding> UTF-8 </ project .build.sourceEncoding>
    </ свойства >

    < зависимости >
        < зависимость >
            < groupId > org.junit.jupiter </ groupId >
            < artifactId > junit-jupiter </ artifactId >
            < версия > 5.4.2 </ версия >
            < область > тест </ область >
        </ зависимость >
    </ зависимости >

    < сборка >
        < плагины >
            < плагин >
                < groupId > org.apache.maven.plugins </ groupId >
                < artifactId > maven-surefire-plugin </ artifactId >
                < версия > 2.22.2 </ версия >
                < конфигурация >
                    < failIfNoTests > истина </ failIfNoTests >
                </ конфигурация >
            </ плагин >
            < плагин >
                < groupId > org.jacoco </ groupId >
                < artifactId > плагин jacoco-maven </ artifactId >
                < версия > 0.8.5 </ версия >
                < казни >
                    < казнь >
                        < id > подготовить агент </ id >
                        < фаза > инициализировать </ фаза >
                        < цели >
                            < цель > агент подготовки </ цель >
                        </ цели >
                    </ выполнение >
                    < казнь >
                        < id > отчет </ id >
                        < фаза > проверить </ фаза >
                        < цели >
                            < цель > отчет </ цель >
                        </ цели >
                    </ выполнение >
                </ казни >
            </ плагин >
        </ плагины >
    </ build >
</ проект >

